---
classes:
  - role::dockerhost
profile::docker_containers::containers:
  tensorflow:
    image: "tensorflow/tensorflow"
    ports:
      - "8888:8888"
    env:
      - PASSWORD=somepassword
    use_name: true
